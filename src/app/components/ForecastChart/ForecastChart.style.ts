import { colors } from "@/theme"

export const styles = {
	container: {
		width: "100%",
		maxWidth: 1100,
		height: 340,
		margin: "32px auto 0 auto",
		padding: "32px",
		background: colors.background,
		borderRadius: "1rem",
		boxShadow: colors.shadow
	}
}
